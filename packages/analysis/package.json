{
  "name": "@designdebt/analysis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test src/*.test.ts"
  },
  "dependencies": {
    "@designdebt/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}

